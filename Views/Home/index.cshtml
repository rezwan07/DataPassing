@model IEnumerable<DataPassing.Models.Person>

@*@model DataPassing.Controllers.Person*@


<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
        @*<h1>hello @TempData["Name"]</h1>

              <h3> hello @Model.Name</h3>
              <h5>  salary @Model.Salary</h5>

            <table border="1px">
                <tr>
                    <th>Name</th>
                    <th>Salary</th>
                </tr>
                @foreach(var item in Model)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td>@item.Salary</td>
                    </tr>
                }

            </table>*@
        <form method="post">
            <table>
                <tr>
                    <td><label>Name</label></td>
                    <td><input type="text" name="name"/></td>
                </tr>
                <tr>
                    <td><label>Salary</label> </td>
                    <td><input type="number" name="salary"</td>
                </tr>
                <tr>
                    <td></td>
                    <td><input type="submit" value="submit"/></td>
                </tr>
            </table>
        </form>
    </div>
</body>
</html>
